# Button example

This example demonstrates how to create and add an interactive button to a capybar bar. The button increments a counter each time it's clicked, showing how to handle user interactions with callbacks.

## Usage

Build the example with
```
cargo build --release --example button
```

Then find executable at `capybar/target/release/examples/button`

Or run the example with
```
cargo run --release --example button
